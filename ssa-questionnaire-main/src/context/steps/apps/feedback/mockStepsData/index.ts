import { mockPagesData } from './mockPagesData';
import { mockQuestionsData } from './mockQuestionsData';

const mockStepsData = [...mockPagesData, ...mockQuestionsData];

export { mockStepsData, mockPagesData, mockQuestionsData };
