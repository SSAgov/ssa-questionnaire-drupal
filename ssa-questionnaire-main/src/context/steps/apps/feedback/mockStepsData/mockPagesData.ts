import { NO } from '@/constants';
import {
  EButtonVariant,
  EQuestionType,
  EStepType,
  IStepsContentItem,
} from '@/interfaces';

export const mockPagesData: IStepsContentItem[] = [
  {
    type: 'page',
    id: 'feedback_thanks',
  },
];
